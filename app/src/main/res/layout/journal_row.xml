<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    >


    <TextView
        android:id="@+id/tvJournalName"
        android:layout_width="254dp"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginEnd="17dp"
        android:clickable="false"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:text="@string/a_journal"
        android:layout_marginRight="17dp" />

    <TextView
        android:id="@+id/tvJournalType"
        android:layout_width="253dp"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/tvJournalName"
        android:layout_below="@+id/tvJournalName"
        android:layout_marginLeft="0dp"
        android:clickable="false"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:text="@string/a_journal"
        tools:layout_width="wrap_content" />

    <ImageView
        android:id="@+id/ivJournalImage"
        android:layout_width="88dp"
        android:layout_height="64dp"
        android:contentDescription="@string/a_journal"
        app:srcCompat="@android:drawable/ic_input_get"
        android:clickable="false"
        android:focusable="false"
        android:focusableInTouchMode="false"/>
    <!--android:onClick="clickEvent"-->

</RelativeLayout>
